DATABASE_URL="postgresql://spelling_bee:spelling_bee@localhost:5432/spelling_bee_dev?schema=public"
TEST_DATABASE_URL="postgresql://spelling_bee:spelling_bee@localhost:5433/spelling_bee_test?schema=public"
NODE_ENV=development
PORT=5000
JWT_SECRET=change-me-in-production
# Must match the JWT_SECRET used by apps/auth (and apps/murder-mystery-voter) — all three
# services verify tokens issued by the shared auth service, so the secret has to line up.
FRONTEND_URL=http://localhost:5173
