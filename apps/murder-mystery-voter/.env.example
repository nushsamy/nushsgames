DATABASE_URL="postgresql://murder_mystery:murder_mystery@localhost:5434/murder_mystery_voter_dev?schema=public"
TEST_DATABASE_URL="postgresql://murder_mystery:murder_mystery@localhost:5435/murder_mystery_voter_test?schema=public"
NODE_ENV=development
PORT=5001
JWT_SECRET=change-me-in-production
# Must match the JWT_SECRET used by apps/auth (and apps/spelling-bee) — all three services
# verify tokens issued by the shared auth service, so the secret has to line up.
FRONTEND_URL=http://localhost:5173
RESEND_API_KEY=re_your_key_here
EMAIL_FROM="Murder Mystery Voter <mysteries@yourdomain.com>"
